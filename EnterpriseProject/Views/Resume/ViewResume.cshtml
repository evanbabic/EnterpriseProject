@model EnterpriseProject.Entities.Resume

@{
	ViewData["Title"] = "View Resume";
}

@if (Model != null)
{
	<h1>@(Model.User.UserName)'s Resume</h1>

	<iframe src="@Model.RelativeFilePath" width="100%" height="800"></iframe>
}

else
{
	<p>No resume found.</p>
}
